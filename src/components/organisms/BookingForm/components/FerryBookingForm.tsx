import React from "react";

/**
 * FerryBookingForm - Placeholder component
 *
 * This component was previously fully commented out as it's not currently in use.
 * The ferry booking functionality is now handled by FerrySearchForm.
 *
 * This can be reimplemented in the future if a separate ferry-specific booking form is needed.
 */
export function FerryBookingForm() {
  return (
    <div
      style={{
        padding: "20px",
        border: "1px dashed #ccc",
        borderRadius: "8px",
        textAlign: "center",
        color: "#666",
      }}
    >
      <h3>Ferry Booking Form</h3>
      <p>This component is currently not implemented.</p>
      <p>Ferry bookings are handled through the main booking flow.</p>
    </div>
  );
}

export default FerryBookingForm;
